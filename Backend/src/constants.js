const defaultMessage = "Something went wrong!";

const transactionModuleName = {
  PartyPurchaseBill: "Party Purchase Bill",
};

const Constants = {
  defaultMessage,
  transactionModuleName,
};

export default Constants;
